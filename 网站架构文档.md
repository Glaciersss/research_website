# 数字人文课题组网站 — 前后端架构文档

## 一、项目总览

| 项目 | 用途 | 技术栈 | 默认端口 |
|------|------|--------|----------|
| `art-culture-backend` | 后端 API 服务 | Python Flask + SQLAlchemy + SQLite | 5000 |
| `art-culture-react` | 公开展示门户网站 | React 18 + Vite 6 + Tailwind CSS 3 | 5173 |
| `art-culture-admin` | 管理后台 | React 18 + Vite 6 + React Router v7 | 5174 |

后端为两个前端统一提供 REST API，数据库使用 SQLite（`app.db`）。

---

## 二、公开展示前端（art-culture-react）

### 2.1 路由机制

**不使用 React Router**。所有页面切换通过 `App.jsx` 中的 `useState` 状态变量驱动，是一个**状态机模式**。一次仅渲染一个视图。

五个状态变量：

| 状态 | 类型 | 作用 |
|------|------|------|
| `selectedMemberId` | `string \| null` | 非 null → 渲染成员详情页 |
| `selectedProjectId` | `string \| null` | 非 null → 渲染成果详情页 |
| `selectedNewsId` | `string \| null` | 非 null → 渲染新闻详情页 |
| `showAllNews` | `boolean` | true → 渲染全部新闻列表页 |
| `showAllProjects` | `boolean` | true → 渲染全部成果列表页 |

### 2.2 页面 / 视图一览

#### 主页（默认视图，所有状态为 null/false）

单页滚动布局，包含 7 个区块，通过页面顶部导航栏的锚点链接平滑滚动：

| 区块 | 锚点 ID | 组件 | 数据来源 | 说明 |
|------|---------|------|----------|------|
| 首屏轮播 | `#home` | `Hero` | `GET /api/public/hero-bg` | 轮播背景图，每 5s 切换，含"了解更多"→`#about`、"研究方向"→`#research` 按钮 |
| 关于我们 | `#about` | `About` | 静态内容 | 研究团队介绍 + 主任寄语 |
| 研究方向 | `#research` | `Research` | 静态内容 | 4 个研究方向：数字化、理解与分析、再创作、展陈 |
| 学术成果 | `#projects` | `Projects` | `GET /api/projects` | 筛选（全部/论文/项目/课程），展示前 3 条，点卡片→成果详情，"查看更多"→全部成果列表 |
| 团队成员 | `#team` | `Team` | `GET /api/team` | 筛选（全部/导师/在读生/毕业生），导师卡片→外部网站，学生卡片→成员详情 |
| 新闻动态 | `#news` | `News` | `GET /api/news` + `GET /api/notices` + `GET /api/events` | 左栏新闻（前 3 条），右栏通知公告 + 投稿日历 |
| 联系我们 | `#contact` | `Contact` | 静态 + `POST /api/contact` | 地址信息 + 留言表单 |

#### 成员详情页（`selectedMemberId !== null`）

- 组件：`MemberDetail`
- 数据：`GET /api/team/:id`，关联成果通过 `GET /api/projects/batch`
- 内容：头像、姓名、角色、研究方向、邮箱、个人介绍、已有成果列表（可点击跳转到成果详情）
- 返回：点击"返回团队列表"→清除状态并滚动到 `#team`

#### 成果详情页（`selectedProjectId !== null`）

- 组件：`ProjectDetail`
- 数据：`GET /api/projects/:id`，关联成员通过 `GET /api/team/batch`
- 内容：类型标签、标题、作者、关联成员（可点击→成员详情）、PDF/Code/DOI 链接、摘要、关键词、配图、详细内容、引用格式
- 返回：点击"返回学术成果"→清除状态并滚动到 `#projects`

#### 新闻详情页（`selectedNewsId !== null`）

- 组件：`NewsDetail`
- 数据：`GET /api/news/:id`，关联成员和项目
- 内容：类型、日期、标题、作者（可点击→成员详情）、配图、正文、关联论文
- 返回：点击"返回新闻动态"→清除状态并滚动到 `#news`

#### 全部成果列表页（`showAllProjects === true`）

- 组件：`ProjectList`
- 数据：`GET /api/projects?limit=100`
- 内容：垂直列表展示所有成果，每条可点击→成果详情
- 返回：点击"返回主页"→清除所有状态

#### 全部新闻列表页（`showAllNews === true`）

- 组件：`NewsList`
- 数据：`GET /api/news?limit=100`
- 内容：垂直列表展示所有新闻，每条可点击→新闻详情
- 返回：点击"返回主页"→清除所有状态

### 2.3 导航流转逻辑

```
主页（7 区块单页滚动）
  ├─ 导航栏锚点 → 平滑滚动到对应区块（#home / #about / #research / #projects / #team / #news / #contact）
  ├─ Hero CTA 按钮 → 滚动到 #about / #research
  ├─ 团队成员卡片 → 设置 selectedMemberId → 切换到成员详情页
  │   └─ 成员详情页中的关联成果 → 设置 selectedProjectId → 切换到成果详情页
  ├─ 学术成果卡片 → 设置 selectedProjectId → 切换到成果详情页
  │   └─ 成果详情页中的关联成员 → 设置 selectedMemberId → 切换到成员详情页
  ├─ 新闻条目 → 设置 selectedNewsId → 切换到新闻详情页
  │   └─ 新闻详情页中的关联成员 → 设置 selectedMemberId → 切换到成员详情页
  ├─ "查看更多学术成果" → 设置 showAllProjects=true → 切换到全部成果列表
  │   └─ 列表中的成果 → 设置 selectedProjectId → 切换到成果详情页
  ├─ "查看更多新闻动态" → 设置 showAllNews=true → 切换到全部新闻列表
  │   └─ 列表中的新闻 → 设置 selectedNewsId → 切换到新闻详情页
  └─ 所有返回按钮 → 清除所有状态 + 平滑滚动到对应区块

页脚快速链接 → 锚点滚动到对应区块
```

### 2.4 组件层级

```
App
├─ Navbar（始终渲染，固定顶部）
├─ [条件渲染：以下六种视图之一]
│   ├─ 默认主页：Hero + About + Research + Projects + Team + News + Contact
│   ├─ 成员详情：MemberDetail
│   ├─ 成果详情：ProjectDetail
│   ├─ 新闻详情：NewsDetail
│   ├─ 全部成果：ProjectList
│   └─ 全部新闻：NewsList
├─ Footer（始终渲染）
└─ BackToTop（始终渲染，右下角悬浮按钮）
```

---

## 三、管理后台前端（art-culture-admin）

### 3.1 路由机制

使用 **React Router v7**（`<BrowserRouter>` + `<Routes>` 模式）。

路由表定义在 `App.jsx` 中：

| 路径 | 组件 | 权限 | 说明 |
|------|------|------|------|
| `/login` | `Login` | 公开 | 未登录时唯一可访问的页面；已登录自动重定向到 `/` |
| `/` | `Dashboard` + `<Outlet>` | 需登录 | 布局壳，默认子路由渲染 NewsManagement |
| `/news` | `NewsManagement` | 需登录 | 新闻管理（CRUD） |
| `/projects` | `ProjectsManagement` | 需登录 | 学术成果管理（CRUD） |
| `/team` | `TeamManagement` | 需登录 | 团队成员管理（CRUD + 拖拽排序） |
| `/notices` | `NoticesManagement` | 需登录 | 通知公告管理（CRUD） |
| `/events` | `EventsManagement` | 需登录 | 投稿日历管理（CRUD） |
| `/hero-settings` | `HeroSettings` | 需登录 | 首页轮播大图设置 |
| `/*` | 重定向 | — | 已登录→`/`，未登录→`/login` |

### 3.2 认证机制

- 登录凭证存储在 `localStorage`：`token`（JWT）+ `user`（管理员信息）
- 每次请求通过 Axios 拦截器自动附加 `Authorization: Bearer <token>`
- 401 响应自动清除凭证并重定向到 `/login`
- 默认账号：`admin` / `admin123`

### 3.3 布局组件：Dashboard

`Dashboard` 既是布局壳也是父路由组件，结构：

```
Dashboard
├─ 侧边栏（250px，深色背景 #2c3e50）
│   ├─ 标题："管理后台" + 欢迎语
│   ├─ 菜单项（均为 React Router <Link>）：
│   │   ├─ /news       → 新闻管理
│   │   ├─ /projects   → 学术成果管理
│   │   ├─ /team       → 团队成员管理
│   │   ├─ /notices    → 通知公告管理
│   │   ├─ /events     → 投稿日历管理
│   │   └─ /hero-settings → 首页大图设置
│   ├─ 外部链接 → localhost:5173（公开展示站，新标签页打开）
│   └─ 退出登录按钮
└─ 主内容区
    ├─ 统计栏（新闻数、成果数、成员数、未读留言）
    └─ <Outlet /> → 渲染子路由对应的管理页面
```

### 3.4 各管理页面功能

| 页面 | 路径 | 功能 |
|------|------|------|
| NewsManagement | `/` `/news` | 新闻 CRUD：标题、类型（投稿日历/研究成果/展览活动/团队动态）、日期、内容、配图、关联成果/成员 |
| ProjectsManagement | `/projects` | 成果 CRUD：标题、类型（论文/项目/课程）、作者、年份、链接、摘要、关键词、配图、出版信息、详细内容、关联成员、PDF/代码链接 |
| TeamManagement | `/team` | 成员 CRUD：姓名、角色（教授/研究员/博士生/硕士生/本科生等）、类别（导师/在读生/毕业生）、研究方向、邮箱、网站、头像、个人介绍、关联成果；支持拖拽排序 |
| NoticesManagement | `/notices` | 通知 CRUD：标题、链接、日期 |
| EventsManagement | `/events` | 投稿日历 CRUD：标题、日期、时间、地点 |
| HeroSettings | `/hero-settings` | 轮播图管理：上传图片（推荐 1920×1080）、预览、删除、保存、恢复默认 |

### 3.5 组件层级

```
App
├─ [loading] → 加载中...
└─ [!loading]
    ├─ [未登录]
    │   ├─ /login → Login
    │   └─ 其他 → 重定向到 /login
    └─ [已登录]
        ├─ / /news → Dashboard → NewsManagement
        ├─ /projects → Dashboard → ProjectsManagement
        ├─ /team → Dashboard → TeamManagement
        ├─ /notices → Dashboard → NoticesManagement
        ├─ /events → Dashboard → EventsManagement
        ├─ /hero-settings → Dashboard → HeroSettings
        ├─ /login → 重定向到 /
        └─ 其他 → 重定向到 /
```

---

## 四、后端 API 总览

所有 API 基础路径由 Vite 代理转发到 `http://localhost:5000`。

### 4.1 公开接口（无需认证）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/public/hero-bg` | 获取首页轮播图列表 |
| GET | `/api/news` | 新闻列表（分页） |
| GET | `/api/news/:id` | 新闻详情 |
| GET | `/api/projects` | 成果列表（可按 type 筛选，分页） |
| GET | `/api/projects/:id` | 成果详情 |
| GET | `/api/projects/batch?ids=` | 批量获取成果 |
| GET | `/api/team` | 团队成员列表（可按 category 筛选） |
| GET | `/api/team/:id` | 成员详情 |
| GET | `/api/team/batch?ids=` | 批量获取成员 |
| GET | `/api/notices` | 通知公告列表 |
| GET | `/api/events` | 投稿日历列表 |
| POST | `/api/contact` | 提交联系表单留言 |

### 4.2 管理接口（需 JWT 认证）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/admin/login` | 管理员登录 |
| GET | `/api/admin/verify` | 验证 token 有效性 |
| GET | `/api/admin/stats` | 仪表盘统计数据 |
| GET/PUT | `/api/admin/settings/hero-bg` | 轮播图获取/保存 |
| POST | `/api/admin/upload` | 文件上传（图片/PDF） |
| POST/PUT/DELETE | `/api/admin/news` `/api/admin/news/:id` | 新闻 CRUD |
| POST/PUT/DELETE | `/api/admin/projects` `/api/admin/projects/:id` | 成果 CRUD |
| POST/PUT/DELETE | `/api/admin/team` `/api/admin/team/:id` | 成员 CRUD |
| PUT | `/api/admin/team/sort` | 成员排序 |
| POST/PUT/DELETE | `/api/admin/notices` `/api/admin/notices/:id` | 通知 CRUD |
| POST/PUT/DELETE | `/api/admin/events` `/api/admin/events/:id` | 事件 CRUD |

---

## 五、数据实体关系

```
News（新闻）
  ├─ related_project_ids → Projects（学术成果）
  └─ related_member_ids → Team（团队成员）

Projects（学术成果）
  └─ related_member_ids → Team（团队成员）

Team（团队成员）
  └─ achievements → Projects（学术成果，JSON 数组存储 project ID）

Notices（通知公告）  — 独立实体
Events（投稿日历）   — 独立实体
Contact（留言）      — 独立实体
```

---

## 六、关键设计决策

1. **公开展示站无路由**：使用 useState 状态机代替 React Router，无浏览器前进/后退、无深层链接、无 URL 变化
2. **无 UI 组件库**：两个前端均为原生 CSS/Tailwind 样式，无 Ant Design/Element UI 等第三方组件库
3. **无懒加载**：所有组件静态 import，无代码分割
4. **数据库**：SQLite 单文件（`app.db`），零配置，适合单机部署
5. **认证**：JWT token 存 localStorage，Axios 拦截器自动附加
6. **文件上传**：图片和 PDF 通过 `/api/admin/upload` 上传，存后端 static 目录
